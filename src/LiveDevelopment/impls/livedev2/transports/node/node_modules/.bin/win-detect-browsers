#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../win-detect-browsers/bin/detect-browsers" "$@"
  ret=$?
else 
  node  "$basedir/../win-detect-browsers/bin/detect-browsers" "$@"
  ret=$?
fi
exit $ret
